import React from 'react';

const App = () => {
    return (
        <div>
        Hi There!
        </div>
    // above is a component - a functional component that returns jsx
    );
};

export default App; //export default syntax